# 비터케어 웹앱 GitHub 업로드 안내

이 폴더는 비터케어 웹앱 V24를 기반으로 한 전체 소스입니다.

## 포함된 내용

- 한국어·영어·중국어·일본어 화면
- 견종 선택 및 견종별 이미지
- 15문항 기질 체크와 맞춤 행동 가이드
- 15개 OX 퀴즈
- 물어뜯기 맞춤 케어
- 사진을 포함한 7일 기록 화면 및 API 소스
- 반응형 모바일 디자인과 이미지 자료

## GitHub에 올리는 방법

1. GitHub에서 `bittercare-webapp`이라는 새 저장소를 만듭니다.
2. 가능하면 처음에는 `Private` 저장소로 설정합니다.
3. 이 ZIP 파일의 압축을 풉니다.
4. 압축을 푼 폴더의 파일과 하위 폴더를 새 저장소에 업로드합니다.
5. `.env` 파일이나 서비스 비밀번호는 GitHub에 올리지 않습니다.

## 중요

- 이 소스는 현재 웹앱의 전체 개발 소스이며 단순 HTML 묶음이 아닙니다.
- GitHub Pages는 정적 호스팅이므로 현재의 사진 업로드와 7일 기록 API를 그대로 실행할 수 없습니다.
- 고객의 7일 기록은 Cloudflare D1에, 사진은 Cloudflare R2에 저장하도록 구성되어 있습니다.
- 관리자 화면은 `/admin`에서 접속하며, 비밀번호는 서버 환경변수 `ADMIN_PASSWORD`로 관리합니다.
- `node_modules`, 빌드 결과물, 임시 파일, Git 이력, 환경변수 파일은 패키지에서 제외했습니다.
